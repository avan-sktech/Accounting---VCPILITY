<!-- ═══ SECURITY — Your Data, Protected ═══ -->
<section class="py-24 px-6 lg:px-10" style="background:#0d1117;">
    <div class="max-w-5xl mx-auto">
        <div class="text-center mb-14 reveal">
            <p class="section-label mb-4" style="color:#c9a84c;">Your Data, Protected</p>
            <h2 class="syne font-bold leading-tight" style="color:#f0ead6; font-size:clamp(1.8rem,3.5vw,2.6rem);">
                Security built for practices that handle <em style="font-style:italic; color:#2dd4bf;">sensitive client data</em>.
            </h2>
        </div>

        <div class="grid md:grid-cols-2 gap-5 reveal">
            <!-- Card 1 -->
            <div class="sec-card">
                <div class="sec-icon">🔒</div>
                <h3 class="sec-title">Encrypted in Transit and at Rest</h3>
                <p class="sec-desc">All client data is protected using industry-standard encryption protocols.</p>
            </div>
            <!-- Card 2 -->
            <div class="sec-card">
                <div class="sec-icon">🛡</div>
                <h3 class="sec-title">Role-Based Access Controls</h3>
                <p class="sec-desc">Granular permissions ensure each team member only accesses what they need.</p>
            </div>
            <!-- Card 3 -->
            <div class="sec-card">
                <div class="sec-icon">📋</div>
                <h3 class="sec-title">Full Activity Logging</h3>
                <p class="sec-desc">Every action is logged with timestamps — complete audit trails from day one.</p>
            </div>
            <!-- Card 4 -->
            <div class="sec-card">
                <div class="sec-icon-text">AU</div>
                <h3 class="sec-title">Australian-Owned Company</h3>
                <p class="sec-desc">Sharktech Global Pty Ltd — ABN 54 672 825 731 — Liverpool, NSW 2170.</p>
            </div>
        </div>
    </div>
</section>

<style>
    .sec-card {
        background: rgba(240,234,214,0.03);
        border: 1px solid rgba(240,234,214,0.08);
        border-radius: 12px;
        padding: 1.8rem;
        transition: border-color 0.3s;
    }
    .sec-card:hover {
        border-color: rgba(45,212,191,0.25);
    }
    .sec-icon {
        font-size: 1.3rem;
        margin-bottom: 0.8rem;
    }
    .sec-icon-text {
        font-family: 'Syne', sans-serif;
        font-size: 0.7rem;
        font-weight: 800;
        color: #2dd4bf;
        background: rgba(45,212,191,0.1);
        border: 1px solid rgba(45,212,191,0.2);
        width: 32px;
        height: 32px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 0.8rem;
    }
    .sec-title {
        font-family: 'Syne', sans-serif;
        font-size: 1rem;
        font-weight: 700;
        color: #f0ead6;
        margin-bottom: 0.5rem;
    }
    .sec-desc {
        font-size: 0.85rem;
        color: rgba(240,234,214,0.5);
        line-height: 1.6;
    }
</style>
